import React from "react";

const Loading = () => {
  return (
    <div className="w-full fixed h-screen flex justify-center items-center bg-white opacity-50">
      <span>Loading...</span>
    </div>
  );
};

export default Loading;
